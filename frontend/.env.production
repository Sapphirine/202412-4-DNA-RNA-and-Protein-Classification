ENV = 'production'

# API base URL (note the protocol: use http instead of https if SSL is not configured)
VUE_APP_BASE_API = 'http://127.0.0.1:8000'

# If the API uses http, update wss to ws
VUE_APP_WS_API = 'wss://localhost:8000'
